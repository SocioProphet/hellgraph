# Mac bundle rebuild and read-kernel addition

This phase rebuilt the GitHub-ready repository as a complete workspace and added `hg_read_kernel`.

Additions:
- real per-crate manifests
- full workspace assembly
- `hg_read_kernel` read-side snapshot summaries
- bundle-friendly transfer docs for macOS

Pending:
- canonical 26-slot basis
- real operator semantics
- query facade and RDF-star bridge
