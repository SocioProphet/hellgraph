// The `hypercore` binding is an optional dependency and (for our purposes) needs no
// bundled types. HypercoreBackend uses its own structural interface and only needs the
// bare module to resolve as `any`.
declare module 'hypercore'
