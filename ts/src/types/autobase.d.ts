// autobase + corestore are optional deps loaded via dynamic import. FederatedAtomSpace
// uses its own structural interfaces, so the bare modules only need to resolve as `any`.
declare module 'autobase'
declare module 'corestore'
