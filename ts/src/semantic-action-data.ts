// GENERATED by scripts/gen-semantic-action.mjs from ontology/semantic-action/SemanticAction.json
// — do not edit by hand. SemanticAction 0.1.0 · 8974 bytes.
// Provenance (see ontology/semantic-action/PROVENANCE.md):
//   repo    SourceOS-Linux/sourceos-spec (merged PR #210)
//   path    schemas/SemanticAction.json
//   commit  ee7e43a42d5b3c30897eee296832ca127e8f6099
//           (2026-07-29, "schemas: KnowledgeNugget + SemanticAction v0.1 — L2 content grain +
//            typed-action registry")
//   sha256  fd01c834f5ca9bbaf524ea777a37cdd7fecdb3a12468b46b18de3244bc741eda
// The digest is asserted at import by ts/src/nlq.ts: a drifted or hand-edited copy fails LOUDLY at
// load, never silently at declareAction time.

/** The vendored SemanticAction JSON Schema, verbatim (raw upstream bytes, trailing newline included). */
export const SEMANTIC_ACTION_SCHEMA_TEXT = "{\n  \"$schema\": \"https://json-schema.org/draft/2020-12/schema\",\n  \"$id\": \"https://schemas.srcos.ai/v2/SemanticAction.json\",\n  \"title\": \"SemanticAction\",\n  \"description\": \"A declarative, typed action registration for the NL→plan compiler: the planner discovers, instantiates, and composes actions by TYPE — ontology URIs over inputs and output, with subsumption constraints — never by executing them. Polymorphism works via subClassOf: a value is bindable to a slot when its type is a subclass of the slot's typeRef, and constraints add further subsumption requirements. Normative purity rule (search-time purity): actions are side-effect-free at plan-search time — search may bind and score an action with zero world change. Any world-changing execution MUST go through the MPCC effect lifecycle (EffectRequest → EffectDecision → EffectRecord; decision before action): sideEffects = \\\"effect-request\\\" declares that the registered executor emits an EffectRequest and defers to an EffectDecision rather than acting directly; sideEffects = \\\"none\\\" declares a pure lookup/computation. There is deliberately no vocabulary for direct world mutation.\",\n  \"type\": \"object\",\n  \"additionalProperties\": false,\n  \"required\": [\n    \"id\",\n    \"type\",\n    \"specVersion\",\n    \"name\",\n    \"version\",\n    \"inputs\",\n    \"output\",\n    \"constraints\",\n    \"executorRef\",\n    \"sideEffects\",\n    \"registry\"\n  ],\n  \"properties\": {\n    \"id\": {\n      \"type\": \"string\",\n      \"pattern\": \"^urn:srcos:semantic-action:[A-Za-z0-9._~-]+$\",\n      \"description\": \"Stable URN identifier of this action registration. Pattern: urn:srcos:semantic-action:<local-id>. Invariant: registration identity is stable and never reused; a changed signature is a new version, not a rewritten registration.\"\n    },\n    \"type\": {\n      \"const\": \"SemanticAction\",\n      \"description\": \"Discriminator constant — always \\\"SemanticAction\\\".\"\n    },\n    \"specVersion\": {\n      \"const\": \"0.1.0\",\n      \"description\": \"SemanticAction contract version, pinned per the policy-integrity tranche-0001 discipline. This family is versioned independently of the v2 metadata-plane schemas; changing this const is a contract change.\"\n    },\n    \"name\": {\n      \"type\": \"string\",\n      \"pattern\": \"^[a-z][a-z0-9_.]*$\",\n      \"description\": \"Stable machine name of the action within the registry (dot-namespaced lowercase, e.g. kb.lookup_filing_metric). Names are the human/registry handle; planners bind by id.\"\n    },\n    \"version\": {\n      \"type\": \"string\",\n      \"pattern\": \"^[0-9]+\\\\.[0-9]+\\\\.[0-9]+$\",\n      \"description\": \"Semantic version of THIS action's signature (inputs, output, constraints, effect posture), independent of the family specVersion const. Any signature change bumps this version.\"\n    },\n    \"inputs\": {\n      \"type\": \"array\",\n      \"items\": {\n        \"type\": \"object\",\n        \"additionalProperties\": false,\n        \"required\": [\n          \"name\",\n          \"typeRef\",\n          \"required\",\n          \"cardinality\"\n        ],\n        \"properties\": {\n          \"name\": {\n            \"type\": \"string\",\n            \"pattern\": \"^[A-Za-z][A-Za-z0-9_]*$\",\n            \"description\": \"Bindable input name, unique within this action. Constraint subjects reference this name.\"\n          },\n          \"typeRef\": {\n            \"type\": \"string\",\n            \"format\": \"uri\",\n            \"minLength\": 1,\n            \"description\": \"Ontology concept URI (KKO reference concepts recommended) typing the values bindable to this input. Binding rule: a value binds when its type is a subclass of (or equal to) this concept.\"\n          },\n          \"required\": {\n            \"type\": \"boolean\",\n            \"description\": \"Whether the planner must bind this input for the action to be applicable.\"\n          },\n          \"cardinality\": {\n            \"type\": \"string\",\n            \"enum\": [\n              \"one\",\n              \"many\"\n            ],\n            \"description\": \"Whether this input binds exactly one value (one) or a set of values (many).\"\n          }\n        },\n        \"description\": \"One typed input slot of the action.\"\n      },\n      \"uniqueItems\": true,\n      \"default\": [],\n      \"description\": \"The action's typed input slots. Required (empty allowed — generator actions may take no inputs) so the signature is always fully declared. Invariant: input names are unique within an action (enforced by the family validator across examples).\"\n    },\n    \"output\": {\n      \"type\": \"object\",\n      \"additionalProperties\": false,\n      \"required\": [\n        \"typeRef\",\n        \"cardinality\"\n      ],\n      \"properties\": {\n        \"typeRef\": {\n          \"type\": \"string\",\n          \"format\": \"uri\",\n          \"minLength\": 1,\n          \"description\": \"Ontology concept URI typing the action's output. For sideEffects = \\\"effect-request\\\" actions this is the proposal the executor emits (e.g. the EffectRequest contract URI), never the world state it might eventually change.\"\n        },\n        \"cardinality\": {\n          \"type\": \"string\",\n          \"enum\": [\n            \"one\",\n            \"many\"\n          ],\n          \"description\": \"Whether the action produces exactly one value (one) or a set of values (many).\"\n        }\n      },\n      \"description\": \"The action's typed output. Exactly one output per action at v0.1; multi-output actions decompose into multiple registrations.\"\n    },\n    \"constraints\": {\n      \"type\": \"array\",\n      \"items\": {\n        \"type\": \"object\",\n        \"additionalProperties\": false,\n        \"required\": [\n          \"kind\",\n          \"subject\",\n          \"typeRef\"\n        ],\n        \"properties\": {\n          \"kind\": {\n            \"type\": \"string\",\n            \"enum\": [\n              \"subClassOf\",\n              \"instanceOf\",\n              \"sameAs\"\n            ],\n            \"description\": \"Constraint kind. subClassOf: the subject's type must be a subclass of typeRef — this is how polymorphism works at plan-search time. instanceOf: the bound value must be an instance of typeRef. sameAs: the subject must denote the same ontology individual as typeRef. The kind taxonomy is closed at v0.1; widening it is a contract bump.\"\n          },\n          \"subject\": {\n            \"type\": \"string\",\n            \"minLength\": 1,\n            \"description\": \"The input name this constraint applies to, or the literal string \\\"output\\\" for the action's output. Resolution against declared input names is enforced by the family validator.\"\n          },\n          \"typeRef\": {\n            \"type\": \"string\",\n            \"format\": \"uri\",\n            \"minLength\": 1,\n            \"description\": \"Ontology concept or individual URI the constraint holds against.\"\n          }\n        },\n        \"description\": \"One typed applicability constraint, checked at plan-search time against the ontology — no execution involved.\"\n      },\n      \"uniqueItems\": true,\n      \"default\": [],\n      \"description\": \"Typed applicability constraints evaluated at plan-search time. Required (empty allowed) so producers state the constraint posture explicitly, per the tranche-0001 bar.\"\n    },\n    \"executorRef\": {\n      \"type\": \"string\",\n      \"pattern\": \"^urn:srcos:[a-z0-9-]+:[A-Za-z0-9._~-]+$\",\n      \"description\": \"URN of the executor bound to this action (e.g. a SkillManifest, Connector, or WorkloadSpec). The registration is declarative; the executor is where the declared sideEffects posture is realized and audited.\"\n    },\n    \"sideEffects\": {\n      \"type\": \"string\",\n      \"enum\": [\n        \"none\",\n        \"effect-request\"\n      ],\n      \"description\": \"Effect posture of the registered executor. Actions are side-effect-free at plan-search time in both cases. \\\"none\\\": a pure lookup/computation — safe to execute without governance hops. \\\"effect-request\\\": the executor does NOT change the world itself; it emits an EffectRequest, and any actual change happens only after an EffectDecision approves it, recorded as an EffectRecord (decision before action). There is deliberately no \\\"direct\\\" value: an action that mutates the world without the effect lifecycle is unregistrable.\"\n    },\n    \"registry\": {\n      \"type\": \"object\",\n      \"additionalProperties\": false,\n      \"required\": [\n        \"owner\",\n        \"deprecated\"\n      ],\n      \"properties\": {\n        \"owner\": {\n          \"type\": \"string\",\n          \"minLength\": 1,\n          \"description\": \"Authority reference of the actor accountable for this registration (human subject, team, agent, or service). Subject or Agent Registry URNs are recommended; free-form identifiers are permitted in v0.1.\"\n        },\n        \"deprecated\": {\n          \"type\": \"boolean\",\n          \"description\": \"Whether this registration is deprecated. Deprecated actions remain resolvable for replaying old plans but MUST NOT be offered to new plan searches.\"\n        }\n      },\n      \"description\": \"Registry metadata: the accountable owner and the deprecation flag. Both required so ownership and lifecycle state are never ambiguous.\"\n    }\n  }\n}\n"

/** SHA-256 over the raw upstream bytes of SemanticAction.json. Pinned — asserted at import. */
export const SEMANTIC_ACTION_SCHEMA_SHA256 = "fd01c834f5ca9bbaf524ea777a37cdd7fecdb3a12468b46b18de3244bc741eda"

/** The `specVersion` const the contract pins. Changing it upstream is a contract change. */
export const SEMANTIC_ACTION_SPEC_VERSION = "0.1.0"
